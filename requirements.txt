# Full stack: retrieval package + classification notebook extras.
# From the repo root:
#   pip install -r requirements.txt
#
# Retrieval CLI / eval / unit tests only (no wordcloud/plotly/jupyter):
#   pip install -e ".[dev]"
-e ".[notebook]"
